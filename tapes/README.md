# tapes

VHS tape scripts for recording llm-cli demo GIFs.

Requires [vhs](https://github.com/charmbracelet/vhs).

```sh
make        # render all scenes
make -j6    # render in parallel
make clean  # remove GIFs
```
